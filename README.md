# Healthcare Assessment App

An app for health risk prediction based on fitness tests.

## Setup
1. Install requirements: `pip install -r requirements.txt`
2. Train model: `python train_model.py`
3. Run app: `streamlit run app.py`